using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainCharacterSkills : MonoBehaviour {

	// Use this for initialization
	void Start () {
       

        /*
       

       





        GetComponent<RPGActor>().PlayerSkills.Add(initiateBattle);
        GetComponent<RPGActor>().PlayerSkills.Add(swordCure);
        GetComponent<RPGActor>().PlayerSkills.Add(attackBoost);
        GetComponent<RPGActor>().PlayerSkills.Add(backAttack);
        GetComponent<RPGActor>().PlayerSkills.Add(fireAttack);
        //image.sprite = Resources.Load<Sprite>( "Images/test" );

    */

    }

    // Update is called once per frame
    void Update () {
		
	}
}
